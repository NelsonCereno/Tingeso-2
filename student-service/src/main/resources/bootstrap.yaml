spring:
  application:
    name: student-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
  datasource:
    url: jdbc:postgresql://localhost:5432/bdmicros
    username: postgres
    password: bajoterra
  jpa:
    hibernate:
      ddl-auto: update
